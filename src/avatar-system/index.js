// Ooka Avatar System — metadata entry point.
// Single import surface for the metadata-driven avatar renderer.
import anchors from './config/avatarAnchors.json'
import layers from './config/avatarLayers.json'
import assets from './config/avatarAssets.json'
import measurements from './config/avatarMeasurements.json'

export { anchors, layers, assets, measurements }
export const AVATAR_SYSTEM_VERSION = '1.0'
